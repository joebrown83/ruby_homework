Joe Brown
ror Homework Assignment#3

You should be able to execute everything just fine by first running the db_generator.rb file to create the university_db.yml file.  Then you can run the db_client2.rb to run the queries.

The recipe.rb file now works with the www added to the beginning of the uri_base
